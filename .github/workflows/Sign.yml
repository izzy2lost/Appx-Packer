name: Sign MSIX Package

on:
  push:
    branches:
      - main  # or whichever branch you prefer to trigger this workflow

jobs:
  sign-msix:
    runs-on: windows-latest

    steps:
    # Step 1: Checkout the Appx-Packer repository
    - name: Checkout Appx-Packer repository
      uses: actions/checkout@v4

    # Step 2: Unpack MSIX package using 7zip
    - name: Unpack MSIX package using 7zip
      run: |
        # Unpack the MSIX file
        & "C:\Program Files\7-Zip\7z.exe" x sdl-min_1.0.0.0_x64.msix -oextracted

    # Step 3: Sign MSIX package using Appx-Packer
    - name: Sign MSIX package using Appx-Packer
      run: |
        # Run Appx-Packer to sign the MSIX package
        Start-Process -FilePath "$GITHUB_WORKSPACE\Appx-Packer\Appx-packer.exe" -ArgumentList `
          "/input $GITHUB_WORKSPACE\extracted", `
          "/output $GITHUB_WORKSPACE\signed_output\signed_sdl-min_1.0.0.0_x64.msix", `
          "/cert $GITHUB_WORKSPACE\certificate.pfx", `
          "/password 1330" `
          -Wait

    # Step 4: Upload the signed MSIX package
    - name: Upload signed MSIX package
      uses: actions/upload-artifact@v4
      with:
        name: signed-msix
        path: $GITHUB_WORKSPACE\signed_output\signed_sdl-min_1.0.0.0_x64.msix
